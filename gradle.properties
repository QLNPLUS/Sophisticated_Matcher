# Gradle 运行参数
org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1G
org.gradle.daemon=false

minecraft_version=1.20.1
forge_version=47.4.20
mod_id=sophisticated_matcher
mod_name=Sophisticated Matcher
mod_license=ARR
mod_version=1.3.0
mod_group_id=com.sophisticatedmatcher
mod_authors=QLN
mod_description=Use item NBT rules as item-id-independent Sophisticated filters, and combine several matchers with AND, BUT, or OR.
sophisticated_core_version=1.20.1-1.3.6.1514

# Forge 1.20.1 需要 JDK 17。
# 请把 JAVA_HOME 指向 JDK 17 后运行 gradlew；GitHub Actions 会自动配置对应 JDK。

